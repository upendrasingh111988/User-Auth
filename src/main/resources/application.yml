spring:
  name: Auth-User
  datasource:
    url: jdbc:mysql://localhost:3306/authUser
    username: root
    password: Upen@1988
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379